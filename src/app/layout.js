// src/app/layout.js
import '../styles/globals.css';

export const metadata = {
  title: 'Smart Emergency Hospital Dashboard',
  description: 'Real-time hospital emergency management system with live ambulance tracking, resource management, and alert coordination.',
  keywords: 'hospital, emergency, dashboard, ambulance, tracking, ICU, real-time',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <meta charSet="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
        {/* Google Fonts — must be <link> not @import in Next.js */}
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous" />
        <link
          href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap"
          rel="stylesheet"
        />
        {/* Bug 10 fix: Leaflet CSS as link tag — prevents FOUC from @import inside style tag */}
        <link
          rel="stylesheet"
          href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
          integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY="
          crossOrigin=""
        />
      </head>
      <body>{children}</body>
    </html>
  );
}
